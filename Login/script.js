const register = document.querySelector('.sign-up-link')
const login = document.querySelector('.log-in-link')
const registerForm = document.querySelector('.form-section-register')
const loginForm = document.querySelector('.form-section-login')
register.addEventListener('click', () => {
    if(registerForm == 'display: none'){
        registerForm == 'display: flex'
    } else {
        
    }
});